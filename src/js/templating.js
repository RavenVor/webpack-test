import template from '../templates/gallery-items.hbs';

const sckils = ['HTML', 'CSS', 'JS', 'REACT'];
const markap = template(sckils);

// const items = ['HTML', 'CSS', 'JS', 'REACT'];

// const galleryRef = document.querySelector('.gallery__list');
// galleryRef.insertAdjacentHTML('beforeend', markap.join(''));

// const element = `<li><a href="">Hello</a></li>`;

// const galleryItems = items.map(el => `<li><a href="">${el}</a></li>`).join('');

// galleryRef.insertAdjacentHTML('beforeend', galleryItems);
